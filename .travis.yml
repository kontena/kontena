language: ruby
rvm:
  - 2.2
cache: bundler
addons:
  apt:
    sources:
      - mongodb-upstart
    packages:
      - mongodb-org-server
env:
  - TEST_DIR=server
  - TEST_DIR=agent
  - TEST_DIR=cli
script: CI=true cd $TEST_DIR && bundle install && bundle exec rspec spec/
